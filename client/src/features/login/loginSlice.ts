import { createSlice } from "@reduxjs/toolkit"

const loginSlice = createSlice({
    name: 'login',
    initialState: {},
    reducers: {
        login(/*state, action*/) {
            return {

            }
        },
        logout() {

        }
    }
})

